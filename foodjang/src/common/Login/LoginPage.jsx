import {useContext, useState} from "react";
import {Link} from "react-router-dom";
import { LoginContent } from "./LoginContent";

export default function LoginPage(){

    const {logout,id,pw}=useContext(LoginContent);
    
}